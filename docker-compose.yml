services:
  backend:
    build: ./backend
    container_name: backend_container
    ports:
      - "4000:4000"
    volumes:
      - ./backend:/app
      - /app/node_modules
    environment:
      - MONGO_URI=mongodb+srv://sajii:Saji2000@cluster0.kvs6cuy.mongodb.net/food_delivery_app

  frontend:
    build: ./frontend
    container_name: frontend_container
    ports:
      - "5173:5173"
    depends_on:
      - backend
    stdin_open: true
    tty: true
    volumes:
      - ./frontend:/app
      - /app/node_modules

  admin:
    build: ./admin
    container_name: admin_container
    ports:
      - "5174:5174"
    depends_on:
      - backend
    stdin_open: true
    tty: true
    volumes:
      - ./admin:/app
      - /app/node_modules